# PRISM — runtime configuration
# Paste your Groq API key on the line below (no quotes).
# Get one free at https://console.groq.com/keys
GROQ_API_KEY=

# Optional — Flask port (default 5000)
# PORT=5000